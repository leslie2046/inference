# Deprecated in Xinference 3.0.
# Model and inference-engine dependencies are installed into per-model virtual
# environments at first launch; they are intentionally not baked into the
# CUDA 12.4 slim image.
